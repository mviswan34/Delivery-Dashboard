<div class="flex flex-col items-center pt-16">
  <h1 class="font-bold text-4xl">Viewing all pending Orders </h1>
  <div class="grid gap-4 mt-8">
    <% @available_orders.each do |order| %>
      <%= render "available_order", order: order %>
    <% end %>
  </div>
</div>
